# 后续 AI 协作说明

## 项目目标

本仓库用于制作一份面向中间件开发岗位的中文教程，最终部署到 GitHub Pages。请后续 AI 在继续工作前阅读本文件和 `docs/index.md`，把本文件视为用户长期要求的摘要。

当前进度与剩余任务见 `TODO.md`：用户要求已识别的改进项**分步骤完成，一次只做一项**，做完再做下一项。

## 目标读者

读者是**约 1 年工作经验**的 C++ 软件开发者：

- 能写 C/C++ 代码，接触过现代 C++，但对语言细节和工程实践尚不熟练。
- 能在 Linux 上开发和调试，了解常用系统调用，但缺少系统级设计经验。
- 了解 Socket、TCP/UDP 和基本网络编程。
- **了解**多线程、锁和常见并发问题，但没有大量并发编程实战。
- 没有系统的机器人通信中间件开发经验。

因此教程需要：

- 不假设读者熟练掌握并发、内存模型和内核机制；关键背景要**补足并解释清楚**。
- 术语首次出现时给出中英文对照和通俗解释。
- 从"为什么需要"讲到"怎么做"，再到"什么时候会出错"，避免只列结论。
- 但也不要退化成 C++ 语法教材：讲基础是为了服务中间件设计、实现、调试和性能工程。

## 目标岗位能力

教程必须围绕 `JobDescription-amap.md` 中的真实招聘要求持续完善，覆盖以下能力：

1. 单机、多机、云边端和多智能体场景下的高性能、低延迟通信。
2. 发布订阅、请求响应、服务调用、节点发现、状态同步、QoS、消息路由和服务治理。
3. 图像、点云、IMU、控制指令、状态反馈和模型推理结果等高频或大带宽数据流。
4. ROS 2、DDS、Cyber RT、DORA、Zenoh、ZeroMQ 等框架的原理、边界和技术选型。
5. Protobuf、FlatBuffers、IDL、ROS bag、MCAP，以及序列化、反序列化和版本兼容。
6. 数据采集、缓存、压缩、索引、分片、回放、同步上传和异常恢复。
7. DAG、Taskflow、计算图、依赖触发、时间同步和接口抽象。
8. Multi-Agent 的状态共享、任务协同、意图交换、能力发现、拓扑管理和分布式决策。
9. 超时、重试、断线重连、限流、降级、容错恢复、链路追踪和故障定位。
10. 围绕延迟、吞吐、抖动、CPU、内存、磁盘 I/O、网络带宽和链路稳定性的性能优化。

## 教程组织要求

教程入口是 `docs/index.md`，章节文件采用以下形式：

```text
docs/index.md
docs/chapter1.md
docs/chapter2.md
...
docs/chapter11.md
```

`docs/index.md` 必须持续维护：

- 岗位要求到章节的映射。
- 章节学习顺序和前置关系。
- 每章统一结构说明。
- 综合实战项目和能力验收标准。
- 指向已构建页面的章节导航链接。

每个章节至少包含以下段落：

1. 本章目标与前置知识。
2. 为什么需要这项技术（动机与问题背景）。
3. 核心概念和术语（首次出现即解释）。
4. 原理深入（机制、推导、时序图或状态机图）。
5. 工程实现（完整可编译代码与逐段讲解）。
6. 常见错误与陷阱（错误写法与正确写法对比）。
7. 与机器人、自动驾驶、分布式系统或高性能数据链路相关的真实案例。
8. 动手实验、测试方法和可观察的验收标准。
9. 本章小结与自查清单。
10. 常见面试问题与准确、具体、有取舍的参考答案。
11. 延伸阅读。

篇幅目标：教程整体应达到可出版书籍的深度，每章内容充实、能独立支撑学习，而不是提纲式罗列。

## 内容质量标准

后续 AI 改进时应优先增加真正能形成能力的内容，而不是重复关键词：

- 说明正常路径、线程模型、队列、内存、网络和存储边界。
- 对每个机制说明失败路径：慢消费者、丢包、重复、乱序、断网、重启、磁盘满和版本不兼容。
- 明确“可靠”“实时”“高性能”“零拷贝”等词的边界、代价和测试条件。
- 给出容量估算、延迟分解、p50/p95/p99、吞吐、CPU、内存、带宽和磁盘指标。
- 结合 C++/Linux 的实现方式，但不要把教程变成某个框架 API 手册。
- 真实案例应解释问题、根因、方案、取舍和验证方式；不能只写抽象口号。
- 面试答案应能体现设计判断，避免绝对化表述，例如“可靠 QoS 永远更好”或“共享内存天然零拷贝”。
- 发现技术事实可能随版本变化时，注明适用框架/版本，并建议通过官方文档和实验验证。

## 综合实战方向

持续完善一个“机器人数据总线实验室”作为贯穿项目，至少包含：

- IMU、图像、点云和控制指令的不同 QoS。
- 同机 IPC、共享内存和跨机网络。
- 发布订阅、服务调用、发现、消息路由和有界队列。
- 慢消费者隔离、背压、丢旧、去重、超时和断线重连。
- Protobuf 或 FlatBuffers 的 schema 演进和跨版本测试。
- MCAP 或 ROS bag 录制、压缩、索引、崩溃恢复和回放。
- Multi-Agent 心跳、能力发现、状态快照/增量、任务租约、epoch 和弱网注入。
- trace、性能基准、故障注入和性能报告。

## GitHub Pages 约束

- `docs/_config.yml` 是 GitHub Pages/Jekyll 配置入口，不要随意删除。
- 所有教程页面保留 Jekyll front matter，例如 `layout: default` 和 `title`。
- `docs/index.md` 中的站内章节链接使用构建后的 `.html`，例如 `chapter1.html`，以保证 Pages 部署后可访问。
- 保持 Markdown 表格、代码块、列表和中文标题可以被 Kramdown 正常渲染。
- 不要把教程入口改成只适用于本地编辑器的路径。

## 后续修改规则

- 先阅读 `docs/index.md` 和要修改的章节，再进行局部增量修改。
- 保留用户已有内容；不要无理由重写或删除章节。
- 新增章节时同步更新 `docs/index.md` 的映射、路线和链接。
- 修改后检查 Markdown 诊断、front matter、站内链接和文件状态。
- 不要提交无关的格式化、依赖或个人配置文件。
- 不要自动创建 Git commit，除非用户明确要求。
- 如需引入外部事实、框架行为或版本信息，优先核对官方资料，并在内容中标明假设。

## 期望的改进方向

后续可以按优先级继续增强：

1. 为章节增加更完整的 C++ 代码、接口定义和可运行实验。
2. 增加 ROS 2/DDS、Zenoh、Cyber RT、DORA、ZeroMQ 的对比实验。
3. 增加 MCAP/ROS bag 的具体数据格式和恢复实现。
4. 增加性能测试脚本、trace 示例和故障注入工具。
5. 增加系统设计图、时序图、状态机图和容量计算示例。
6. 增加每章练习题、参考实现、常见错误和进阶阅读。
7. 检查教程中技术结论是否与目标框架版本一致。
